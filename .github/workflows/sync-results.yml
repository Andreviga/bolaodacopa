name: Sync results

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync ge results to Supabase
        run: |
          curl --fail --show-error --silent "https://copammv.vercel.app/api/sync-results?source=github-actions"
